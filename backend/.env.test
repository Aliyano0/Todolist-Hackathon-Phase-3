# Test environment configuration for Docker container
DATABASE_URL=postgresql://test:test@host.docker.internal:5432/test_db
JWT_SECRET_KEY=test-secret-key-minimum-32-characters-long-for-security-testing-purposes
JWT_ALGORITHM=HS256
JWT_EXPIRY_DAYS=7
BETTER_AUTH_SECRET=test-better-auth-secret-minimum-32-characters-long-for-testing

# SMTP Configuration (test values)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=test@example.com
SMTP_PASSWORD=test-password-placeholder
SMTP_FROM_EMAIL=noreply@test.com
SMTP_FROM_NAME=Todo App Test
SMTP_USE_TLS=true
SMTP_TIMEOUT=30

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Server Configuration
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000
WORKERS=1

# Security and Performance
RATE_LIMIT_ENABLED=false
RATE_LIMIT_PER_MINUTE=60
LOG_LEVEL=INFO
LOG_FORMAT=json

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000
